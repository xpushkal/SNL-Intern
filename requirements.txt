# --- API layer ---
fastapi==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.10.4

# --- LLM (Groq) + agent orchestration ---
groq==0.13.1
langgraph==1.2.7
langchain-core==1.4.8

# --- Retrieval: dense (local embeddings) + sparse (BM25) + fuzzy name match ---
sentence-transformers==5.6.0
transformers==5.12.1  # explicit floor: <5.3 has known CVEs
rank-bm25==0.2.2
rapidfuzz==3.11.0
numpy==1.26.4

# --- utils / eval ---
python-dotenv==1.2.2
httpx==0.28.1
pytest==9.1.1
