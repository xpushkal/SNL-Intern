# --- API layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- LLM (Groq) + agent orchestration ---
groq==0.13.1
langgraph==0.2.60
langchain-core==0.3.28

# --- Retrieval: dense (local embeddings) + sparse (BM25) + fuzzy name match ---
sentence-transformers==3.3.1
rank-bm25==0.2.2
rapidfuzz==3.11.0
numpy==1.26.4

# --- utils / eval ---
python-dotenv==1.0.1
httpx==0.28.1
pytest==8.3.4
